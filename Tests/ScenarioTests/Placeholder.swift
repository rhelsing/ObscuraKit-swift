import XCTest

// Placeholder so SPM can find this test target.
// Scenario tests will be added in Layer 5.
final class PlaceholderTests: XCTestCase {
    func testPlaceholder() {
        XCTAssert(true)
    }
}
